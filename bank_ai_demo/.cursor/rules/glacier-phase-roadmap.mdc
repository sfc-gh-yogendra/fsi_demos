---
description: Glacier First Bank demo roadmap across Phases 1–3 with self-contained build specs
alwaysApply: false
---
# Glacier First Bank — Demo Roadmap (Phases 1–3)

This rule documents the end-to-end roadmap so implementation can proceed without relying on chat history.

## Phase 1 — Foundation (approved)
- **Scenarios**: AML/KYC (EDD) and Credit Analyst (loan origination assessment)
- **Agents**: `aml_officer_agent`, `credit_analyst_agent`
- **Standards**: See [glacier-phase1-defaults.mdc](mdc:glacier-phase1-defaults.mdc) and [agent-templates-glacier.mdc](mdc:agent-templates-glacier.mdc)
- **Data scope**: Demo-scale; generate only what’s necessary for stories; EU/EBA tone; EUR (en‑GB format)
- **Search services**: compliance_docs_search_svc, credit_policy_search_svc, loan_documents_search_svc (include ON, ATTRIBUTES, WAREHOUSE, TARGET_LAG)
- **Semantic views**: `credit_risk_sv` (ratios/cohorts) and optional `customer_risk_sv` (light AML context)
- **Acceptance**: Story flows complete (3–4 turns), citations and dates present, thresholds flagged (⚠️/🚨), SEARCH_PREVIEW works, SEMANTIC_VIEW queries validated, audit summary available

## Phase 2 — Expansion (Relationship & Wealth)
- **Objectives**: Broaden business coverage, introduce cross-entity reasoning, keep performance demo-grade
- **New Scenarios**:
  1. **Corporate Relationship Manager (RM)** — Client intelligence & proactive opportunity sourcing
     - **Agent**: `corp_rm_agent`
     - **Semantic View**: `corporate_client_360_sv` (customers, transactions, vendor_relationships)
     - **Search Service**: `client_documents_search_svc` (call notes, internal emails, news; attrs: id, title, content, source_type, client_name, publish/created_date)
     - **Story (3–4 turns)**: risks/opportunities summary → shared vendor exposure → pre‑meeting briefing → trend visual on request
     - **Wow visual**: stacked bar of risks/opportunities by source_type for last 60 days
  2. **Wealth Advisor** — Portfolio alignment and what‑if rebalancing
     - **Agent**: `wealth_advisor_agent`
     - **Semantic View**: `wealth_client_sv` (client CRM + holdings + model portfolios; metrics: equity_allocation, concentration)
     - **Search Service**: `wealth_notes_search_svc` (meeting notes)
     - **Custom Tool (UDF/SP)**: `portfolio_modeler_tool` (simulate trims, tax impact, projected return/risk)
     - **Story (3–4 turns)**: alignment check → rebalance simulation → tax/volatility impact → optional notes summary
     - **Wow visual**: KPI vs threshold for concentration with colour flag; optional before/after allocation chart

- **Data scope**: Add minimal synthetic CRM, transactions, vendor links, holdings, and notes needed for flows
- **Standards**: Reuse Phase 1 defaults (citations, visuals policy, thresholds), adapt tone to persona
- **Acceptance**: Each scenario runs independently in ~15 minutes; visuals render on implied prompts; citations and dates present

## Phase 3 — IBD & Advanced Due Diligence
- **Objectives**: Showcase hybrid structured+unstructured reasoning at scale and legal clause extraction
- **New Scenarios**:
  1. **M&A Analyst — Target Screening**
     - **Agent**: `ma_analyst_agent`
     - **Semantic View**: `ma_screening_sv` (company_financials; dims: industry, geography, revenue_band; metrics: ev_ebitda, revenue_cagr)
     - **Search Service**: `ma_research_search_svc` (industry reports, patent filings; attrs include company_name, publish_date)
     - **Story**: criteria-driven longlist (structured) → confirm tech fit via research (search) → generate strategic rationales
     - **Wow visual**: bar chart ranking strategic fit score by candidate
  2. **Due Diligence — VDR Interrogation**
     - **Agent**: `dd_agent`
     - **Search Service**: `vdr_search_svc` (supplier contracts, litigation, minutes; attrs: id, title, content, document_category, publish_date)
     - **Processing**: AI_PARSE_DOCUMENT + AI_CLASSIFY pipeline to populate `document_category` and extract key fields
     - **Story**: find change‑of‑control clauses → summarise outstanding litigation with potential liabilities → audit trail
     - **Wow visual**: counts by document_category for quick triage

- **Data scope**: Minimal but realistic VDR corpus and company financials sufficient to satisfy flows
- **Standards**: Same response/planning defaults; citations and dates; SQL on request
- **Acceptance**: SEARCH_PREVIEW relevance validated; clause snippets returned with citations; flows complete within ~15 minutes each

## Cross‑Phase Build Principles
- **Multi‑tool orchestration**: Agents may combine Cortex Analyst, Cortex Search, and custom tools per turn
- **Config knobs**: run_mode (mini/demo/full), seed, model (default llama3.1‑70b), language=tone, regulatory_region='EU', per‑doc‑type counts
- **Quality gates**: linkage integrity, title length, min words, avoid apologetic phrasing, unique synonyms in semantic views
- **Performance**: Prefer demo-scale corpora; ensure P95 responses feel snappy for on‑stage use
- **Auditability**: Always offer audit summary; reveal SQL on request (UI-handled)

## Object Naming Summary (consistent across phases)
- **Database**: `BANK_AI_DEMO`
- **Warehouses**: `BANK_AI_DEMO_COMPUTE_WH`, `BANK_AI_DEMO_SEARCH_WH`
- **Agents**: named per scenario as listed above
- **Semantic Views**: `{domain}_sv` naming (e.g., `credit_risk_sv`, `corporate_client_360_sv`)
- **Search Services**: `{purpose}_search_svc` naming (e.g., `credit_policy_search_svc`)

